/** Mobile drawer offset — matches TopNav rendered height. */
export const TOP_NAV_OFFSET_PX = 49;

/** Full-height chart pages subtract this from the viewport. */
export const TOP_NAV_HEIGHT_REM = "3.5rem";

/** Shared max width for authenticated app pages and top nav content. */
export const APP_CONTENT_MAX_WIDTH_CLASS = "max-w-screen-2xl";

/** Centered app content column — use on nav and main wrappers. */
export const APP_CONTENT_CONTAINER_CLASS = `mx-auto w-full min-w-0 ${APP_CONTENT_MAX_WIDTH_CLASS}`;
